# Project: Claude Promotion Clock
# Started: 2026-04-02
# Sessions since last maintenance: 4

## Session 1
- Built project harness: docs, feature list, init/check scripts, PowerShell wrappers, app scaffold
- Implemented: timezone-aware React dashboard with live clock, forecast card, daily signal strip, weekly heatmap, and historical timeline infographic
- Files changed: package.json, src/, docs/, feature_list.json, claude-progress.txt
- Tested: npm run check, production build, local HTTP 200 probe from the dev server
- Harness improvements: added README and Windows-friendly init/check scripts
- Next: replace demo history with real observations or connect a live telemetry source
- Issues: there is no official Anthropic promotions feed in the repo, so the current forecast is explicitly dataset-driven

## Session 2
- Replaced the seeded demo dataset with a real web-backed Express API that scrapes official Claude Help Center promotion articles from the sitemap
- Ported the runtime UI to the user-provided Figma-derived design under refs/design while keeping refs/design excluded from lint and runtime imports
- Normalized official promotion articles into campaign and peak/off-peak window records, then localized them in the browser for the clock, charts, heatmap, and history timeline
- Tested: npm run check, direct tsx snapshot fetch, and HTTP endpoint probe after fixing the Express 5 SPA fallback
- Current estimate quality: real computation over official campaign history, but intentionally low confidence because only a small number of published campaigns are available

## Session 3
- Extended the forecast model from campaign-only estimation to hourly phase inference for both next off-peak and next peak windows
- Kept exact official windows whenever a promotion is active, and only fall back to history-based inference when the campaign itself is estimated
- Added an ET weekday/hour probability model over historical official windows and surfaced the new signals in the forecast panel
- Tested: npm run check and direct snapshot inspection showing campaign, nextOffPeak, and nextPeak forecast outputs

## Session 4
- Added US public holiday context to the hourly forecaster using a live holiday API
- Extended the phase model so future off-peak and peak estimates are weighted by ET day-of-week, weekend, hour, and holiday signals
- Tested: npm run check and direct snapshot inspection confirming the new holiday-aware forecast basis

## Session 5
- Ran a full production-style browser audit with Playwright screenshots on desktop and mobile
- Fixed a major accuracy issue by separating official live promotion status from historical pattern inference in the clock panel
- Improved the history table so peak/off-peak phases are explicit and overnight windows show cross-day ranges clearly
- Hardened the backend so holiday API outages no longer break the promotion snapshot
- Added a repeatable Playwright audit script and ignored generated audit artifacts
- Tested: npm run check, live Playwright screenshots, zero console errors, zero failed requests, and simulated holiday API outage fallback

## Session 6
- Maintenance: re-verified the current app state after the audit, kept docs current, and reset the maintenance counter
- Added a GitHub Actions workflow that runs lint/build checks and smoke-tests the backend promotions API on push and pull request
- Prepared the repository for publication on GitHub and pushed the current app state to the remote repository
- Tested: npm run check, direct API response validation, and CI workflow definition review

## Session 7
- Investigated the live Netlify deploy and confirmed the frontend was published without a backend, leaving `/api/promotions` returning 404 in production
- Added Netlify Functions and redirect rules so the existing promotions backend can run on Netlify under the same domain
- Updated project docs and tracking for the Netlify production path
- Tested: npm run check, direct local Netlify function invocation, live `https://claudeclock.netlify.app/api/promotions`, and live browser render after deploy

## Session 8
- Fixed a forecast-design bug where near-term peak and off-peak estimates were incorrectly delayed until the next estimated public promotion campaign
- Changed the hourly phase forecaster to search the next 7 days directly from the historical day-of-week and hour model, while keeping the public campaign estimate as a separate long-range signal
- Clarified the forecast panel label so the long-range card reads as the next public promotion campaign rather than another hourly pressure estimate
- Tested: direct snapshot inspection showing next peak is current/immediate and next off-peak is later the same day, plus npm run check

## Session 9
- Rebuilt the bar chart and heatmap to use only real official window data instead of the old all-history weekday aggregate
- Changed the heatmap ordering to a rolling 7-day calendar range ending on today, so it no longer forces a Monday-to-Sunday layout
- Added explicit no-data handling for hours and days without official window coverage so empty periods do not read as real off-peak signals
- Tested: npm run check and direct dashboard-model inspection confirming the range now runs from 27 Mar through 02 Apr with today last

## Session 10
- Adjusted the forecast semantics so the next peak card points to the next future peak start instead of echoing the current peak block
- Removed the old full-screen loading page, removed outbound Claude Help Center links from the runtime UI, and made the app shell consistently left-aligned with explicit LTR direction
- Kept the near-term off-peak estimate as a heuristic only and validated that it now points to later today while the next peak points to tomorrow
- Tested: npm run check and direct forecast inspection showing next off-peak at 18:00 UTC on 02 Apr 2026 and next peak at 12:00 UTC on 03 Apr 2026
